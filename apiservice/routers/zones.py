from fastapi import APIRouter

router = APIRouter()

# Phase 1 endpoints:
# GET    /zones
# POST   /zones
# PUT    /zones/{id}
# DELETE /zones/{id}
# GET    /zones/{id}/containers
# GET    /zones/stats
# GET    /zones/density
